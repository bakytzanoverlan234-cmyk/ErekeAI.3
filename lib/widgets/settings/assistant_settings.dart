import 'package:flutter/material.dart';

class AssistantSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Assistant Settings Stub")),
    );
  }
}
